{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "echo 'No build step — Vercel compiles api/ serverless functions automatically.'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
